# Copy this file to `.env` or `.env.local` and fill values (do NOT commit real secrets)
# Vite requires client-side env vars to be prefixed with VITE_

# EmailJS configuration (optional) - if you want to keep credentials out of source
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Optional: change dev server port (Vite reads PORT env var)
# PORT=5173
